OpenCV GPU Stream

This C++ program integrates OpenCV with OpenGL to capture video frames, process them using CPU or GPU (OpenCL), and render them as textures in an OpenGL window. It supports real-time GPU acceleration using OpenCL when available.
